# Single known host - not over-engineering a multi-host inventory for one Pi.
# "anakafeel-pi" resolves via the local ~/.ssh/config alias already used for
# all manual SSH access to this machine (same host, same user, same key).
[vaultpi]
anakafeel-pi ansible_user=anakafeel ansible_python_interpreter=/usr/bin/python3
