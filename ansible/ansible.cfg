[defaults]
inventory = inventory.ini
host_key_checking = True
retry_files_enabled = False
# Vault password lives outside the repo entirely - never commit it.
vault_password_file = ~/.vaultpi-vault-pass
